.form-signin {
    max-width: 330px;
    padding: 15px;
    margin: 0 auto;
}

.form-signin .form-control:focus {
    z-index: 2;
}

body { padding-top: 70px; }

a{
  font: inherit
}
.scrollable {
  overflow:auto;
  max-height: 570px;
}
.blockquote {
  border-left: 5px solid gray;
  padding: 2px 10px;
}
.right{
  float: right;
}

